
	<div class="container">
		<div class="row my-5">
			<div class="col-md-4 offset-md-4">
				<div class="underbar">
					<% if params[:user_id] %>
						<h2><%= @user.name %> さんのレッスン</h2>
					<% else %>
						<h2>レッスン一覧</h2>
					<% end %>
				</div>
			</div>
		</div>
		<%= render 'list', lessons: @lessons %>
	</div>

