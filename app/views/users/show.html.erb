<div class="container">

	<div class="row mt-3 mx-5">
		<div class="col-md-4">
			<h3><%= @user.name%>さんのページ</h3>
		</div>

	</div>
	<div class="row">
		<div class="col-md-3 offset-md-9">
			<% if @user == current_user %>
				<%= link_to "編集する", edit_user_path(@user.id), class:"button-submit" %>
			<% end %>
		</div>
	</div>
</div>